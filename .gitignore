# Secrets — never commit real API keys
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Benchmark run history is KEPT (committed) so the all-time scoreboard is
# permanent and survives across sessions. Only ignore local scratch runs:
runs/*.local.json
*.local.json

# OS / editor
.DS_Store
.idea/
.vscode/
